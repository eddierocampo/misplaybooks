---
- name: Managment KVM
  hosts: all
  gather_facts: true
  become: true

  tasks:
    - name: Iniciar ambiente
      community.libvirt.virt:
        name: "{{ item }}"
        state: "{{ action }}"
      loop: "{{ vm_name }}"
